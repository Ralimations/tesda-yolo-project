name: tesda-yolo
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch
  - torchvision
  - cpuonly
  - flask
  - opencv
  - pip:
      - ultralytics>=8.3
